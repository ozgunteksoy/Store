@model IEnumerable<Product>

<div class="display-6 text-center">
    <h1 style="margin-top: 20px">Showcase</h1>
</div>

<div class="row">
    @foreach (var prd in Model)
    {    
        <partial name="_ProductCard" model="prd" />
    }
</div>